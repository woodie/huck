# huck v0.5.2

## Fixed the `.pkg`'s install location

The signed `.pkg` shipped in v0.5.1 installed Huck into
`/Applications/app/Huck.app` instead of `/Applications/Huck.app` like
every other Mac installer. Fixed by setting the install path explicitly.
